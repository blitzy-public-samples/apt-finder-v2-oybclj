# Web application servers
[web]
web1.apartmentfinder.com
web2.apartmentfinder.com

# Database servers
[database]
db1.apartmentfinder.com

# Redis cache servers
[cache]
redis1.apartmentfinder.com

# Load balancer servers
[loadbalancer]
lb1.apartmentfinder.com

# Variables applied to all hosts
[all:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=/path/to/private_key.pem
ansible_become=yes

# Human tasks:
# TODO: Replace placeholder hostnames with actual server hostnames or IP addresses
# TODO: Update the ansible_ssh_private_key_file path with the correct path to the private key
# TODO: Verify and adjust the ansible_user if a different username is used for SSH access